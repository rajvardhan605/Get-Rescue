# Questions to ask (client / Zoho Studio access needed)

A running list of things that came up while reconciling `app/widget.html` against the flow-spec screenshots, where the answer needs either the client's business input or someone with Zoho Studio access to check a real field — not something guessable from the code alone. Add to this file whenever a question like that comes up; check items off (or delete them) once answered, and fold the answer back into `README.md` (§3 confirmed facts, or §4 if it stays a caveat).

This is a companion to `README.md`'s own §4 ("Open questions / unconfirmed assumptions") — that section is the fuller technical log of every guessed field name across the whole file; this file is just the short, current, "needs a human answer" punch list.

---

## Open

- **[2026-08-04] Vendor Assignment dropdown showing raw record IDs — RESOLVED, real field names confirmed from the user's own console dump.** The diagnostic logging added earlier the same day immediately paid off: the user pasted their console output, which showed a real `Vendors_Report` record's exact 9 keys (`service_type`, `Vehicle_Issue`, `Email`, `Address`, `Vendor_Priority`, `Vendor_Type`, `vendor_name`, `Availability_Status`, `ID`). Three corrections made: (1) the name field is lowercase `vendor_name`, not `Vendor_Name` — the planned rename never landed, same surprise as `Vehicle_Issue`'s `VEHICLE_TYPE`; (2) priority is `Vendor_Priority`, not `Priority`; (3) phone is **`Mobile_Number_01`** (this app's own `Mobile_Number_NN` convention), not a plain `Phone` field — user-confirmed the same day. Also discovered: vendor lat/lon are NOT flat `Latitude`/`Longitude` fields — they're nested inside Zoho's own composite `Address` field as `Address.latitude`/`Address.longitude` (added a `vendorLatLon()` helper that reads that first, falling back to flat fields only if `Address` is absent). All fixed in `CONFIG`/`vendorInfo()`, documented in `FIELDS.md`.
- **[2026-08-04] Real bug fixed: Tab jumping to the top for the "Issue" and "Later" (Time_of_service) fields.** Two distinct causes, both fixed: (1) the radio `.opt` divs (used by `Time_of_service`, `Preferred_Vendor`, `Refund_Due`, `B2B_GST_Invoice_Required`) had no `id` attribute at all, so `preservingFocus()` — which restores focus by looking up `document.activeElement.id` after a re-render — could never find them again; picking "LATER" (which reveals `Schedule_Date`/`Service_Time`) lost focus entirely, so the next Tab restarted from the very first field. Every option now gets a stable, deterministic id. (2) `afterMultiselectChange()` (used by `Vehicle_Issue`, i.e. "Issue") called plain `renderWizard()` instead of `rerenderCurrentView()`, bypassing `preservingFocus()` altogether — fixed to go through the same wrapper every other RERENDER-triggering change uses.
- **[2026-08-04] `Service_Time` widened to the full 24-hour day.** Was hardcoded to 9:00 AM–5:00 PM half-hour slots — a customer scheduling an evening/overnight breakdown had no valid slot to pick. Now covers all 48 half-hour slots across the full day.
- **[2026-08-04] Remarks author attribution — RESOLVED, using login email as the stand-in this file itself suggested.** `boot()` now captures `LOGIN_EMAIL` from `ZOHO.CREATOR.UTIL.getInitParams()` (this widget never tracked its own logged-in identity before — no "Agents" master report exists to look up a display name from), and every new Remarks entry is stamped `"<timestamp> — <email>: <text>"` instead of just `"<timestamp> — <text>"`.
- **[2026-08-04] Real bug fixed (best-effort, not independently confirmed): Remarks blank-on-reopen.** The earlier diagnostic logging (below) never got a follow-up console dump, but the symptom — real, timestamped entries visible in the native Zoho record, blank in this widget on reopen — matches a known Zoho Creator quirk: bulk `getRecords()` responses can return long Multi-line field values truncated compared to a direct single-record fetch. `openTicket()` (and the dashboard's own dedicated "Close Case" button) now re-fetch the one record fresh via a new `apiGetById()` before opening it, falling back to the bulk-list copy if that API is unavailable or fails — so this can only improve correctness, never regress it. **Not fully confirmed as THE root cause** since it couldn't be reproduced locally — if Remarks still shows blank after this fix, check the new `"openTicket key fields (after fresh re-fetch)"` console line first; if `Remarks` is already empty there, the bug is elsewhere (most likely the field's real API name, or a genuine server-side clear).
- **[2026-08-04] A ticket scheduled for later now writes `Status:"SCHEDULED"`.** Previously every newly-created ticket wrote `"SERVICE FEE QUOTE"` regardless of `Time_of_service` — a customer who picked "LATER" looked identical to a walk-in "NOW" ticket on the dashboard. `SCHEDULED` was already wired into the "Ready & Invited" dashboard bucket (pre-existing, never actually written by anything) — now `create`'s own `status` resolver writes it when `Time_of_service==="LATER"`, and `quote`'s own resume `statuses` list accepts it so a scheduled ticket still reopens at Quote next, same as before.
- **[2026-08-03] All Zoho forms/reports deleted and rebuilt from scratch — schema is now authored, not guessed.** The user deleted every form/report (they'd accumulated mismatched field names and unnecessary data) and asked for a complete build spec instead: every form, its exact API field names/types/dropdown values, and List/Detail view recommendations per report — now in `FIELDS.md` Parts 1–2, with `ACCESS.md` updated to match. Along the way: `vendors_Report` and the separate `My_Availability_Vendor` form are unified into one `Vendors_Report` (no real reason for a vendor to have two records across two forms); `vendors_Report`'s multi-candidate phone/priority/status/lat/lon/email guesses collapsed into single confirmed field names; `technician_name`/`vendor_name` renamed to `Technician_Name`/`Vendor_Name` for casing consistency; `Vehicle_Issue`'s all-caps `VEHICLE_TYPE` renamed to `Vehicle_Category`; `Task_Rejections.Case_ID` renamed to `RSID` to match `Invites`' own naming; dead fields dropped entirely (`Assign_Technican`/`1`, `Service_Request`/`1`, `Action_field`/`2`, `Assignment_Sent`, `Total_Service_Fee1` — none ever had a real writer); `Payment_Method1` renamed `Payment_Method_Final`. A real bug was also fixed in the same pass: `Invites` only had a `Vendor` Lookup field, but `vendorTicket`'s fleet hand-off was writing a `Technicians_Report` ID into it — a Zoho Lookup can only target one form, so that could never have resolved correctly. `Invites` now has a separate `Technician` Lookup field, and `vendorTicket`/`technicianTicket`/`driverTicket` were updated to write/match on the correct field. `ticketKanbanGetRescue` (which had drifted behind several of `getRescueTicket`'s own earlier fixes) was brought back in sync on the dead-field removals and two lingering type mismatches (`Odometer_Reading` file→number, `Handover_to_Number` tel→number) as part of the same pass, since leaving it out of sync would have broken its own status derivation once the dropped fields no longer exist.
- **[2026-08-03] Real bug fixed: "Cannot set properties of null (setting 'disabled')" — recurring across every action button in all three field widgets.** Every action handler's `finally` block re-enabled its own button (e.g. `$("acceptBtn").disabled=false`) unconditionally — but a *successful* action typically calls `goToStatus()`/navigates away first, which re-renders the whole screen and destroys that button's DOM element before `finally` runs. Setting `.disabled` on `null` then threw an uncaught `TypeError`. Fixed everywhere at once: added a `setDisabled(id, val)` helper (`vendorTicket`/`technicianTicket`/`driverTicket`, near each widget's own `$()` helper) that no-ops if the element doesn't exist, and replaced all ~56 `$("...Btn").disabled=...` call sites with it across the three widgets.
- **[2026-08-03] Real bug confirmed live: `Service_Acceptance_Next` rejects a boolean.** The console showed `["Invalid column value for Service_Acceptance_Next"]` when `acceptService()` sent `true` — confirms this is a Yes/No-style field expecting the literal string `"Yes"`, not a real Boolean/Decision box, matching every other Yes/No field already used in this codebase. Changed to `"Yes"` in all three widgets. Still a guess whether `"Yes"` itself is exactly right (vs. e.g. a different casing or option set) — `updateMyInvite()`'s own `console.warn` will surface it immediately if this attempt also fails, since this write is non-blocking either way.
- **[2026-08-03] "Invalid column value for Status" on Work Completed — diagnostic logging added, cause not yet found.** `workCompleted()` (RSR WIP, in both `vendorTicket` and `technicianTicket`) writes `Status:"REMAINING FEE DUE"` and got this exact error live. Added a payload-logging line (`[GR-Vendor]`/`[Get-Rescue] workCompleted payload:`) plus a full `console.error` of the thrown error in both widgets. Next occurrence: check the console for the exact rejected value and paste back what Zoho's own error says — could be a typo/casing mismatch in the picklist option, or the field expecting something else entirely.
- Separately noticed in the same console dump, not yet investigated: `uploadFile failed for Image_Upload2 Invalid Configuration...!!!` — unclear if this is a real, currently-relevant issue or leftover from an earlier action in the same session; flagging in case it recurs on its own.
- **[2026-08-03] "Invalid column value for Payment_Status" on Payment Received — RESOLVED.** The user shared the real field's live Zoho config: `PAID` / `PENDING` / `NOT APPLICABLE`. This is the SAME field as the Quote step's own `Payment_Status` — not a separate field, resolving the earlier "naming collision" flag too. All three widgets' `renderPaymentScreen()`/`confirmPaymentReceived()` now read/write `PAID`/`PENDING` instead of the guessed `Success`/`Pending`. `getRescueTicket`'s `isPaidFlag()` (fixed a few rounds ago to check for `"PAID"` specifically) was already correct.

- **[2026-08-03] Real bug fixed: "RSP_Start_Lon has exceeded its maximum digits" on Accept.** This is written to `Create_Case` (via `Agent_Ticket_Report`) — the vendor/technician/driver's own GPS position captured at Accept time, in `acceptService()`. Root cause: `getPositionSafe()` (shared by `technicianTicket`/`driverTicket`/`vendorTicket`) passed the browser's raw `navigator.geolocation` coordinates straight through, which can have 15+ decimal places — overflowing whatever Decimal field `RSP_Start_Lat`/`RSP_Start_Lon` (and by the same mechanism, `Cancel_Location_Lat`/`Lon`, `RSP_Drop_Location_Lat`/`Lon`) are on Create_Case. Fixed at the source: `getPositionSafe()` now rounds before returning, so every caller across all three widgets gets a safe value automatically. **Updated 2026-08-03, same day**: the user widened those Decimal fields to 15 places, so the rounding was changed from 6 → 15 decimal places (via `toFixed(15)`, not a `Math.round(x*1e15)` multiply — that can exceed `Number.MAX_SAFE_INTEGER` for lat/lon-sized values and silently corrupt the result). Diagnostic logging was added in each of the three widgets' `acceptService()` (`[Get-Rescue]`/`[GR-Vendor] acceptService payload:` — exact values sent, both as the JS number and its string form) plus a full `console.error` of the actual thrown error.

**Resolved 2026-08-03, same day, differently**: rather than keep tuning the Decimal precision, the user changed `RSP_Start_Lat`/`RSP_Start_Lon` to plain **Text** fields and renamed them `RSP_Start_Latitude`/`RSP_Start_Longitude` — sidesteps the digit-limit issue entirely for these two. Renamed throughout `technicianTicket`/`driverTicket`/`vendorTicket`. **`Cancel_Location_Lat`/`Lon` and `RSP_Drop_Location_Lat`/`Lon` are still Decimal** (only these two were changed) — if the same "maximum digits" error shows up on Cancel or the TOW "Reached Drop Location" step, the diagnostic logging pattern here is the template to reuse.

- **[2026-08-03] RSID column clarification confirmed** — the "RSID" column on `Agent_Ticket_Report` is just a display label for the real `Case_ID` field, not a separate field. No relation to `Invites.RSID` (a different, actually-separate Lookup field on the `Invites` form) — pure naming coincidence, both fixed.
- **[2026-08-03] Real bug fixed: Phone Number blank on reopen for tickets not created via this widget.** `Phone_Number1` (what the Create step's own box reads/writes) is only ever populated by this widget itself, mirrored into the real required `Phone_Number` field on save. Any ticket where `Phone_Number1` was never independently set (created directly in Zoho Studio, imported, etc.) has a real `Phone_Number` but a blank `Phone_Number1` — so reopening it showed an empty box despite the record clearly having a phone number. Fixed: `openTicket()` now falls back to `Phone_Number` if `Phone_Number1` is empty.
- **[2026-08-03] Remarks blank on reopen despite being saved — see the 2026-08-04 entry above for the fix attempted.** User showed a native Zoho record with real, timestamped Remarks entries (matching this widget's own append-only format exactly, so the save clearly worked), but the widget's own Create step showed "No remarks yet." on reopen. Couldn't identify the cause through code review alone — the save and read paths both use the same `Remarks` field name, so this isn't the same class of bug as Phone_Number. Added a targeted console log (`openTicket key fields — ... Remarks:`) — next time this happens, check whether `rec.Remarks` is already empty in that log line (would mean the field's real API name differs from what this widget expects, same class of thing as the RSID/Case_ID labeling) or has content (would mean the bug is in this widget's own `t:"remarks"` rendering instead, which would need this exact log's output to trace further).

- **[2026-08-03] New "Invites" architecture — implemented for Action 5 (Accept/Reject) only, other stages still pending.** Per the user's confirmed answers: one `Invites` record (report `Invites_Report`) is created per invited vendor per ticket by `getRescueTicket`'s own JS (`syncInvites()`, called after the Assignment step saves — duplicate-safe, checks existing invites for that ticket first). `Invites.RSID` (Lookup → `Create_Case`) and `Invites.Vendor` (Lookup → whoever's invited — vendor OR, per "invite covers all", a technician/driver too) are both sent as bare IDs (not wrapped objects — see the earlier Lookup write-shape revert). `vendorTicket`/`technicianTicket`/`driverTicket` now find "my tickets" via `Invites_Report` first (matching `Vendor`'s unwrapped display name against the logged-in person's own name), falling back to the older `Vendor_Emails`/`Technician_Emails`/name-match logic for any ticket that doesn't have a matching Invites record yet. `vendorTicket`'s fleet hand-off (`assignTechnician()`) also creates an Invites record for the picked technician.
  - **Only Action 5/5a (Accept/Reject) writes to the Invites record so far** — `Service_Acceptance_Next: true` on Accept, `{Status:"RSP REJECT", Reject_Reason}` on Reject. The original prompt names several more per-stage flags (`Reach_Breakdown_Location_next`, `Work_In_Progress_Next` / `Work_In_Progress_Loading_For_TOW_Next`, `WIP_Reached_Location_For_TOW_Next`, `WIP_Unloading_For_TOW_Next`) that presumably need the same treatment at Reach/WIP/Loading/Drop/Unloading/Payment — **not yet implemented**, to keep this round to a reviewable size. Tell me once Action 5's Invites behavior is confirmed working live and I'll extend the same pattern through the rest of the flow.
  - **Unconfirmed, flagged rather than guessed further**: (1) `Service_Acceptance_Next`'s exact value format — sent as boolean `true`; could instead need to mirror the Status string (e.g. `"RSP ON THE WAY"`) or something else entirely. (2) The Reject-path field names on `Invites` (`Status`/`Reject_Reason`) are guessed by analogy with `Create_Case`'s own fields, not given directly. (3) `Invites_Report`/vendor-dashboard matching is done by fetching ALL Invites records and filtering client-side by name (not a server-side `criteria` filter on `Vendor`) — deliberately avoided guessing Zoho's exact Lookup-criteria syntax after the "no vendor is assigned" and Lookup-write-shape incidents earlier this session; fine for correctness, just less efficient at scale.

- **[2026-08-03] "Fields show empty when reopening a saved ticket" — couldn't reproduce, needs your console output.** Added diagnostic logging at `openTicket()` (`[Get-Rescue] openTicket raw record from Zoho:` and `...state.record after unwrap:`) and at `prevStep()`/`jumpTo()` (`[Get-Rescue] prevStep/jumpTo -> state.record:`). Please open a ticket you know has saved data, open the browser console, and: (1) check if the field's value is already missing in the very first log line (raw record from Zoho) — if so, that field's API name in the code doesn't match the real one, check it against `FIELDS.md`'s ❓/🟡-marked (unconfirmed) fields; (2) if it IS present there but the screen shows blank, or it disappears after navigating steps, paste me the console output and I'll trace the exact display bug. Every field's `n:` value is listed in `FIELDS.md` step-by-step for cross-checking against your real Zoho schema.

- **[UI pass 2026-07-31] "ON DEMAND" client match is case-insensitive text, not a real flag.** `Client_Tracking_Id` hides when the selected `Client`'s display text equals "ON DEMAND" (trimmed, case-insensitive) — assumes that's a real client-master entry with that exact name, not e.g. a special `Client_Type` field. If there's a dedicated field marking a client as "no tracking ID needed" rather than a literal name match, let me know and I'll switch to that instead.
- **[UI pass 2026-07-31] Remarks entries have no author attribution — RESOLVED 2026-08-04, see above.** The new append-only Remarks log timestamps each entry but doesn't say who added it, since this widget doesn't track the logged-in agent's own display name anywhere (unlike the vendor/technician/driver widgets, which fetch their own profile record). If per-entry attribution matters, I'd need to know where to source the agent's own name from (a report/field, or just their login email as a stand-in).

- **[Lookup write shape — tried an object wrapper, reverted 2026-07-31, same day] Bare ID is what actually works.** Briefly changed every Lookup write (`getRescueTicket`'s `idsFor()`, `ticketKanbanGetRescue`'s copy, and the `Task_Rejections` write in `technicianTicket`/`driverTicket`/`vendorTicket`) to send `{"ID":"..."}` objects instead of a bare ID, based on a reported Zoho response shape. This broke saving entirely — even the Create step stopped submitting. **Reverted the same day** back to plain bare-ID values everywhere, which is what was working before and is working again now. If Lookup writes ever need revisiting, don't reintroduce the object-wrapping without testing live first.

- **[Critical bug found and fixed 2026-07-31] `buildPayload()` still called the deleted `formatVendorForSave()`.** While implementing the new `Vendor_Emails` field (below), found that the MAIN wizard "Save & Continue" path (`buildPayload()`, used by every stage including `assignment`) still had its own, differently-shaped copy of the Vendors1-save logic that called `formatVendorForSave()` — which was deleted earlier this session when `Vendors1` was fixed to be a real Lookup. This would have thrown a `ReferenceError` on every single Assignment-step save (the earlier `replace_all` fix only matched two of the three occurrences, because this one used a different local variable name). Fixed now — this was very likely the actual reason vendor assignment kept failing even after the earlier Lookup fix.
- **[New hidden fields added 2026-07-31, per user request] `Vendor_Emails` and `Technician_Emails`.** Both are plain text fields, comma-separated, auto-populated by the widgets (not shown in any widget's own UI — the user will hide them on the real Zoho form). `Vendor_Emails` is written by `getRescueTicket` whenever `Vendors1` is saved (resolved from `vendors_Report` via a new `email` candidate list). `Technician_Emails` is written by `vendorTicket`'s fleet hand-off, currently just mirroring the one directly-assigned technician's email. Both are now also checked by the corresponding dashboards (`vendorTicket`/`technicianTicket`/`driverTicket`) as a more reliable alternative to the name/email-field guessing used before.
- **[Still open] Fleet hand-off is still direct-assign, not request-then-accept.** For `Technician_Emails` to actually hold multiple candidates (matching how `Vendor_Emails` works for vendors), the fleet hand-off needs to become a genuine multi-select "request several technicians" step before showing a single Assign button — this is the same "technicians multi-lookup" field this file has been asking about; still need its exact API name before building that flow.

- **[Assignment step simplified 2026-07-31, per user request]** The Assignment screen now matches the Action 4 mock exactly: `Preferred_Vendor`, `Vendors1`, `Remarks` only. Moved `Vendor_Email`/`Assigned_Technician`/`Assigned_Technician_Email` to `acceptance`/`acceptanceTow`'s own read-only display (outcome data, not agent input). **Removed `Assignment_Sent` entirely** — no code ever read it, no real notification system is wired to it, and it isn't in the mock; flag if there's a real reason to bring it back. Also dropped `vendorTicket`'s old 3-field email guess (`Vendor_Email`/`Vendor_Email1`/`Vendor_Email2`) for its own dashboard matching — `Vendors1` Lookup-membership matching (already fixed) is more reliable than guessing a field name, so no replacement email field is needed there.

- **[Assigned_Vendor write-side implemented 2026-08-04 — item 1 below resolved by the schema rebuild, item 2 still open.** Originally blocked on two questions:
  1. ~~Does `Assigned_Vendor` lookup to `vendors_Report`, and can `vendorTicket` even resolve its own `vendors_Report` ID?~~ **Resolved**: the 2026-08-03 schema rebuild merged the old separate `My_Availability_Vendor` form into `Vendors_Report` itself, so `vendorTicket`'s own `currentVendorRecord.id` (fetched at boot) already *is* a `Vendors_Report` ID — no email-matching bridge needed. `acceptService()` now sends it as `Assigned_Vendor` on Accept. `loadDashboard()` now also reads it back to implement the Action 4 mock's "1st person to accept, task goes away from other invited vendors' apps" — a ticket past `RSP IRA (INVITE RESPONSE AWAITED)` only keeps showing for the vendor actually recorded in `Assigned_Vendor` (falls back to the old "any invited candidate" behavior if that field is blank, so a ticket accepted before this fix doesn't vanish from everyone). Best-effort only — no server-side lock, so two vendors accepting in the same instant could theoretically both still write it.
  2. **Still open**: the exact API field name for the fleet hand-off's **"technicians multi-lookup"** (the field a fleet vendor would use to *request* several of their own technicians at once, vs. today's direct-assign-one behavior). Until answered, the fleet hand-off (`showHandoffPanel`/`assignTechnician`) still direct-assigns a single technician immediately — no separate request/accept race exists there yet, so it doesn't need the same "goes away from other apps" treatment for now.

- **[vendorTicket] Real bug fixed 2026-07-31: invited vendors saw an empty dashboard.** Reported live: tickets at `RSP IRA (INVITE RESPONSE AWAITED)` clearly showed the logged-in vendor in `Vendors1` (per the Agent Ticket Report) but never appeared in that vendor's own "My Tickets" — because `isMine()` only checked `Vendor_Email`/`Vendor_Email1`/`Vendor_Email2`, none of which are necessarily set yet at the invite stage (only `Vendors1`, the multi-candidate invite list, is). Fixed by also matching the logged-in vendor's own name (`MY_VENDOR_NAME`, read from their `My_Availability_Vendor` record's `vendor_name` field at boot) against each `Vendors1` chip's parsed name. **Needs live verification**: this assumes `vendors_Report`'s own label field (whichever of `Vendor_Name`/`Name`/`vendor_name` `getRescueTicket` actually uses to build `Vendors1`'s chip text) produces the *exact same string* as `My_Availability_Vendor.vendor_name` for the same vendor — if a vendor is still missing from their own dashboard after this fix, check for a mismatch between those two names first (e.g. trailing spaces, different casing, or the two reports using different label fields entirely).

- **[Naming collision, found 2026-07-31] Two different fields are both called `Payment_Status`.** Compiling the full dropdown-values reference in `FIELDS.md` surfaced this: the long-standing `quote`-stage field (booking-fee status: `PAID`/`PENDING`/`NOT APPLICABLE`) and the new field added during the Action 8 round (final-fee status: `Pending`/`Success`, read/written by `technicianTicket`/`driverTicket`/`vendorTicket`'s Payment screen) share the exact same name but have unrelated value sets — they can't both be the same real Zoho field. **Which one (if either) is your actual existing `Payment_Status` field?** Whichever isn't it needs renaming (e.g. the new one → `Final_Payment_Status`) across `getRescueTicket` and all three field widgets before you build the Zoho dropdown for it. See `FIELDS.md`'s own callout at the top of its new "Dropdown / Radio / Select values" section for the full detail.

- **[Action 4] Vendor "Mechanic vs Driver" categorization field** — the flow spec's footnote says "Employee list is filtered by service type: Mechanics for RSR, Drivers for Towing," but `vendors_Report` has no known field for this today, and `vendorsFor()` doesn't filter by it. What's the real field name (and its exact values) that marks a vendor as a Mechanic vs. a Driver, so the Assignment step's picker can filter to the right one for the ticket's `Service_Type`?
- **[Action 4] "APP STATUS" vs "LOGIN STATUS"** — the Assignment popup's mock table has two separate status columns; today this app only tracks one online/offline flag (`CONFIG.vendorFieldCandidates.status`). Explicitly deferred 2026-07-31 ("keep it for now as online offline") — revisit if/when it turns out these need to be two distinct fields (e.g. "logged into the app at all" vs. "currently busy on another job").
- **[vendorTicket] Individual vs. Fleet — now an authored schema decision, not a guess (2026-08-03).** Since `Vendors_Report` is being rebuilt from scratch, `Vendor_Type` (values `"Individual"`/`"Fleet"`) is the field to create there, read once at boot into `IS_FLEET`. Not yet exercised against a live save — if the hand-off panel never appears for a real fleet account (or appears for an individual one), this is the first thing to check.
- **[vendorTicket] `Technicians_Report.Fleet_Vendor`** — links a technician back to their owning fleet vendor, matched against the vendor's own display name (not an ID/Lookup match) in `showHandoffPanel()`. Still open: should this be a real Lookup instead of a name-string match? A string match is more fragile (case/whitespace mismatches silently show an empty technician list) — worth upgrading once there's a real fleet account to test against.
- **[vendorTicket] Exact status semantics at the fleet hand-off moment** — implemented as: Accept always writes `Status:"RSP ON THE WAY"` immediately (same as an individual vendor), then a fleet vendor is shown the hand-off panel on top of that (assigning a technician doesn't change `Status` again — the technician's own widget picks up the ticket via an email match against `Assigned_Technician_Email`/`Technician_Email`/`Technician_Email1`, already at `RSP ON THE WAY`). Not explicitly confirmed — an alternative would be a distinct "awaiting hand-off" status that only becomes `RSP ON THE WAY` once a technician is actually assigned.
- **[driverTicket/vendorTicket] The 4-point roundtrip's "Mechanic" waypoint — RESOLVED 2026-07-31.** The Action 7b mock's own footnote describes the roundtrip as "RESCUE[static] to Breakdown Location[from app] to Drop Location [from app] to RESCUE[static]" — a plain 3-leg Office→Breakdown→Drop→Office trip, with no separate "Mechanic" location at all. Treating "Mechanic" (in the original field-generation spec's own leg names) as just another label for that same static office/"RESCUE" point. No code change needed — `calc4PointRoundtrip()` already produces exactly this 3-leg total (`mechanicToBreakdown` hardcoded `0` folds cleanly into that reading). The field *names* (`Office_To_Mechanic` etc.) still don't match this new understanding but were kept as-is to avoid renaming already-shipped, cross-referenced fields — cosmetic only.
- **[Final Closure] "send all Task data to the RESCUE back-end through API" — not implemented.** The Final Closure mock's own footnote says closing a task should also push its data to an external "RESCUE" backend via API, separate from the normal Zoho Creator record save. `saveClosure()` only does the normal record update today — there's no endpoint, auth, or payload format given for this integration, so nothing was guessed/built. Needs the actual API details (URL, auth, expected payload shape) before this can be implemented.
- **[Payment_Method1] Full option list — RESOLVED 2026-08-03.** User's own Zoho field screenshot confirms the real list is `Cash` / `UPI` / `Card` / `Net Banking` (same as Step 2's own `Payment_Method`), replacing the earlier guessed "Payment Gateway (Default) / Direct / Cash". Since none of these is an automatic online gateway, the QR-code/auto-pay block was removed from all three field widgets' Final Payment screen — every method now requires the receipt photo (or a zero balance) before "Payment Received" unlocks. `QR_Code`/`Send_Payment_Link1` fields are now vestigial (kept, not removed) in the agent-side `payment` stage.
- **[Action 4, follow-up] Does "Final Payment Collection" (`payment` stage) also count as a vendor/technician/driver step?** Per the Screen-by-Screen Flow Atlas, payment collection (QR code / gateway) is performed by the technician/driver on-site, same as Acceptance/Reach/WIP — but this wasn't explicitly re-confirmed when "make vendor steps read-only" was decided (2026-07-31), so `payment`'s fields in the agent widget were deliberately left editable for now pending confirmation, unlike `acceptance`/`reach`/`wip`/`acceptanceTow`/`reachTow`/`wipLoadingTow`/`wipReachedTow`/`wipUnloadingTow` (all locked view-only that same day). Note the new `technicianTicket`/`driverTicket`/`vendorTicket` widgets DO implement Final Payment Collection themselves (per explicit spec) — this question is specifically about the agent-facing `getRescueTicket` widget's own copy of that stage.
- **[Data source] `Task_Rejections` form — now built, field names confirmed 2026-07-31.** The user created this form in Zoho Studio with `Case_ID` and `Vehicle` as real **Lookup** fields (not plain text, as originally guessed) — `RSP_Name`/`Reason`/`Vehicle_Issue` stayed Single Line, `Rejected_At` is Date-Time. Updated all three widgets' write to send `Case_ID:r.ID` (the ticket's own record ID) and `Vehicle:r[CONFIG.vehicleField]` (the raw value as fetched, unconverted) instead of display strings, since Lookup fields need the linked record's ID. **`Vehicle`'s exact write format is unconfirmed/needs live testing** — none of these widgets previously needed this field's raw ID shape, only its display value via `readVal()`, so whether passing it straight through actually works isn't verified. The write stays non-blocking (a logging failure never blocks the actual rejection), so this can be tested live without risk — if it silently fails, check the browser console warning ("Task_Rejections log failed") and this field first.
- **[Camera/GPS/Google Maps — needs real-device or live testing, not just a human answer]** The camera+GPS photo watermark modal (`openCamera`/`capturePhoto` in all three new widgets) can only be verified on a real mobile browser with camera+location permissions granted — untested beyond syntax/logic checks in this build. Same for the Haversine-based `calcDistanceETA`/`calc4PointRoundtrip` stand-ins once a real Google Maps Distance Matrix API key is provided (user confirmed 2026-07-31 they have one but it's not being handed over yet) — they're written as drop-in-replaceable single functions specifically so swapping in the real API call later doesn't require touching every call site.
- **[Action 5] Field name conflict: `ETA`/`Distance_To_Breakdown` vs. "RSP ETA"/"RSP Distance"** — the original field-generation prompt said "Save outputs to: RSR: ETA, Distance_To_Breakdown," already implemented and in `FIELDS.md`. Both the Action 5 (RSR) AND Action 5a (TOW) mocks instead say "calculate ETA (save in 'RSP ETA' field) & Distance (save in 'RSP Distance' field)" — **identical wording for both branches**, which is actually a point *against* these being real per-branch field names (the original spec is explicit that RSR/TOW use separate fields — `ETA`/`Distance_To_Breakdown` vs. `ETA_TOW`/`Distance_To_Breakdown_TOW` — but both mocks describe both branches with the exact same generic phrase, suggesting casual/descriptive language rather than a precise per-branch spec). Kept the original branch-specific names for now — please confirm either way.
- **[Action 5] Status conflict on Reject: `RSP REJECT` vs. `READY FOR ASSIGNMENT` directly — RESOLVED 2026-07-31.** The Action 5a (TOW) mock explicitly and unambiguously states "TASK STATUS will become 'RSP REJECT'" for the Reject button, matching the original prompt and what's already implemented. Treating this as confirmation that the earlier RSR mock's "READY FOR ASSIGNMENT" phrase referred to something else (likely the *later* Back-Office re-assignment outcome, not this button's own direct write) rather than a real conflict — no code change needed, `rejectService()` keeps writing `Status:"RSP REJECT"` everywhere.
- **[Action 5] Back-Office "REASSIGN" button on all RSR/TOW app pages** — mentioned in the original generation prompt, the Action 5 (RSR) mock, the Action 6 mock, and again in this Action 5a (TOW) mock (2026-07-31) — recurring in nearly every screen now. Not yet implemented in `technicianTicket`/`driverTicket`/`vendorTicket` — these are built as field-only apps (no Back-Office role branch), and `getRescueTicket` already gives Back-Office their own full re-assignment flow via the Vendor Assignment step. Needs a decision: is this describing something these three field widgets should also detect and add (would need a real role-detection mechanism — e.g. does the logged-in email match a Technician/Vendor record at all?), or is it describing a Back-Office capability that already exists elsewhere and doesn't need duplicating here?
- **[Action 6] Back-Office-only "Send ETA To Cx via WhatsApp / Copy Msg" button** — same situation as the REASSIGN button above: this is explicitly marked "only back office" in the mock, and these three widgets have no Back-Office role branch. Not implemented — same decision needed as the REASSIGN item.
- **[Action 6/7] Recurring "RSP_" naming pattern — mixed evidence, still open.** Action 6: "RSP Reach time"/"RSP distance" (implemented as separate, non-colliding `Cancel_Distance`, see `FIELDS.md`). Action 7: "saves it in 'RSP Completion time' field" for BOTH Work Completed and Service Reject — renamed directly (`Work_Completed_Time` → `RSP_Completion_Time`) since it wasn't conflicting with an already-shipped name. But Action 5/5a's own "RSP ETA"/"RSP Distance" repeats identically across BOTH RSR and TOW (see the item above) — weakening the case that this is a precise per-field prefix rather than generic phrasing. **Net: `RSP_Completion_Time` is live as a real rename; `ETA`/`Distance_To_Breakdown`/`Reach_Time` remain unchanged pending explicit confirmation.**
- **[Action 6] New guessed fields, none independently confirmed**: `RSP_Start_Lat`/`RSP_Start_Lon` (captured at Accept, Step 5, so Cancel can compute a distance from it), `Cancellation_Time`, `Cancel_Location_Lat`/`Cancel_Location_Lon`, `Cancel_Distance` — all added 2026-07-31, all guessed names, see `FIELDS.md`.
- **[Action 6] "Save the photo in 'vehicle number' photo field"** — read as meaning the arrival/cancel photo should show the vehicle's number plate (a content instruction), not a literal different field name from the already-used `Image_Upload2`/`Image_Upload5` — not confirmed either way; flagging in case a literal separate field was actually intended.
- **[Action 7] "Issue not resolved – add voice note?"** — the mock itself phrases this as a question, suggesting the client isn't fully decided either. Implemented as a plain text `Unresolved_Note` textarea shown when Issue Resolved = No — **not real audio/voice recording** (that would need microphone permission, `MediaRecorder`, and audio file upload — a materially bigger, unconfirmed feature). Confirm whether a text note is sufficient or real voice capture is actually wanted.
- **[Action 6] ETA manual-entry fallback** — the mock says ETA should be "Captured from app click / if null - entered manually." Implemented as an always-editable number input pre-filled with whatever was auto-captured at Accept, which the tech/driver/vendor can freely overwrite before tapping Reached — not gated on it actually being null first (simpler, and does no harm if it's already been set), but flagging the difference from a literal read of the spec.
- **[Action 8] `Transaction_ID`/`Time_Of_Receipt`** — the mock says a real Payment Gateway "success" should capture these; not implemented anywhere, since there's no real gateway webhook in this app to source them from honestly. Add once real gateway integration exists.
- **[Action 6a] Status label "Work in Progress - Loading" vs. shipped `LOADING`** — the Action 6a (TOW) mock says the Reached-Location button's target is "TASK STATUS will become 'Work in Progress - Loading'", but the actual status value used everywhere in this codebase (dashboard `MY_STATUSES`, `STAGES`/`wipLoadingTow` in `getRescueTicket`, `driverTicket`/`vendorTicket`'s own `markReachedTow`) is the plainer `LOADING`, already grouped under a "Work In Progress" bucket label in the agent dashboard. Treated as the same descriptive-bucket-name pattern as the earlier (resolved) "READY FOR ASSIGNMENT" confusion — not changed — but flagging in case `LOADING` really should be a longer literal string.
- **[Not yet fixed] `ticketKanbanGetRescue` has the identical synthetic-field status-derivation bug, untouched.** `ticketKanbanGetRescue/app/widget.html` carries its own near-identical copy of the `acceptance`/`acceptanceTow`/`reach`/`reachTow`/`wipLoadingTow` stage definitions (same `Service_Request`/`Service_Request1`/`Action_field`/`Action_field2` fake fields, same wrong `wipLoadingTow` "Cancel" outcome) — and unlike `getRescueTicket`, these fields aren't even marked `viewOnly` there, so it may still let a Kanban user directly edit fields no vendor-side widget reads. Out of scope for this round (not part of any Action-N mock pasted so far, and wasn't touched this session before now) — flagging so the identical fix (see the `getRescueTicket/README.md` entry) gets applied there too before relying on that widget's own status handling.
- **[Action 7a] Real bug found and fixed: `getRescueTicket`'s vendor-side status derivation was reading fields no widget ever writes.** While reconciling this screen, `wipLoadingTow`'s stage config turned out to derive its "current status" from a synthetic `Action_field2` radio + a `Rejection_Reason1` field that `driverTicket`/`vendorTicket` never actually write (they write the real `Status` field directly) — same bug also found in `acceptance`, `acceptanceTow`, `reach`, `reachTow` (all deriving status from `Service_Request`/`Service_Request1`/`Action_field`, none of which any real widget populates). Since these stages are all `viewOnly:true` (agent can't edit, but clicking "Save" still writes whatever `status()` computes), this could have silently reverted a cancelled/rejected ticket's real `Status` back to its "normal" value the next time an agent saved that stage. **Fixed 2026-07-31**: all five now read `r.Status` directly instead of a fake field, and each field's own conditional `when:` display was switched from checking the fake field to just checking whether that field's own value is present. `wipLoadingTow`'s alternate outcome was also corrected to match this actual mock: "Customer Rejected" → `Status:"REMAINING FEE DUE"` (shared `Rejection_Reason`/`RSP_Completion_Time` fields, same as Step 7's own Cx-reject), not "Cancel" → `RSP CANCELLED` as previously guessed (that Cancel outcome belongs to `reachTow`/Action 6a, a different stage). Also added matching Cx-Reject panels (previously entirely missing) to `driverTicket`'s and `vendorTicket`'s own TOW Loading screens.
- **[Action 7a] Field naming conflict: `Breakdown_To_Drop_Distance`/`Return_Journey_ETA` vs. "Drop Distance"/"DROP ETA"** — same treatment as the still-open Step 5 ETA/Distance conflict: kept the original spec-matrix names, flagging rather than silently renaming, since this is a different name pair proposed by a single mock versus an already-shipped, already-cross-referenced (Step 7b's 4-point total) pair of names.
- **[Action 7a] Behavioral fix: Drop distance/ETA now computed from the driver's live GPS position, not the static breakdown location** — the mock explicitly says "based on current GPS location of Driver," so `vehiclePicked()` in both `driverTicket` and `vendorTicket` now calls `getPositionSafe()` at click time and uses that as the origin point (falling back to the ticket's static `Latitude`/`Longitude` only if a GPS fix isn't available).
- **[Action 7b] New fields: `Drop_Location_Arrival_Time`, `RSP_Drop_Location_Lat`/`Lon`** — added 2026-07-31, both guessed names, captured when "Reached Drop Location" is tapped. Also fixed: `getRescueTicket`'s own `Odometer_Reading` field was displayed as a photo-upload widget instead of a number input (a real display bug — the agent could never actually see the odometer value).
- **[Action 7c] `Handover_to_Number` corrected to a number field** (was a `tel`-type text box) per the mock's explicit "[Number field]"; `RSP_Completion_Time` (the same shared field as Steps 7/7a) is now also stamped when "Dropped" is tapped.
- **[Action 5a, follow-up] `getRescueTicket`'s `acceptanceTow` stage doesn't display the TOW Start Odometer at all** — noticed while checking Step 7b's own `Odometer_Reading` display; `driverTicket`/`vendorTicket` capture a Start Odometer reading on Accept (Action 5a), but `acceptanceTow`'s own field list in `getRescueTicket` has no odometer field to show it (unclear what the real field name is — `Odometer_Reading` is already used for the drop-side reading; may need a distinct start-side field name, or may be the same field reused twice the same way as the drop-side one already is). Flagging rather than guessing a name — same field-reuse pattern as `Odometer_Reading` itself, not yet confirmed for the start side.
- **[Final Closure] Re-confirmed, no changes needed**: Closure_Status stays a 3-option field (`Not Converted`/`Cancelled`/`Completed`, not split into "Cancelled Billable"/"Not Billable") and the permanent Close-Case button placement — both already decided 2026-07-30, and this newer, more detailed mock paste (with the 4-row validation table) doesn't change either answer, since the fee formula is identical across both "Cancelled" rows. `Payment_Status` (added for the field widgets during the Action 8 round) is now also mirrored read-only into this stage's own display, since it wasn't shown to the agent before.
- **[Action 6a] Confirms Action 6, no new fields.** The TOW mock for this same screen (Reach Breakdown Location / Cancel) matches what `driverTicket`/`vendorTicket` already had field-for-field: `Odometer_reading_at_Reached_Location`, `Toll_Charges`, `Reach_Time`, mandatory photo on both Reached/Cancel, `Status:"RSP CANCELLED"` on Cancel, `Cancellation_Time`/`Cancel_Location_Lat`/`Lon`/`Cancel_Distance` — no code changes needed. Its repeat of "RSP Reach time"/"RSP distance" (identical to the Action 6 RSR mock's own phrasing) further reinforces treating this as generic phrasing rather than a literal per-field prefix — see the "RSP_ naming pattern" item above.

---

## Answered (kept here briefly for context; see README §3/§5 for the full writeup)

- Vendor-side wizard steps (Acceptance/Reach/WIP, or their Tow equivalents) are now view-only for the agent — confirmed 2026-07-31: the vendor/technician/driver updates these fields directly in their own native Zoho Creator (there is no separate vendor widget), so this widget just displays what they've entered.
- "Service Provider Assigned" / "Service Provider Assigned Contact Number" (Action 4 mock) are the same thing as the existing `Vendors` chip display + per-row contact info already shown in the Assignment popup — not new fields. Confirmed 2026-07-31.
- **Final Closure's vendor-side rows stay fully click-to-edit** — confirmed 2026-07-31 ("they can edit by themselves on clicking on close ticket thing while closure of ticket also"). Resolves the "Final Closure, follow-up" question that used to be listed above.
- **Data source for the three new widgets is `Create_Case` only** — confirmed 2026-07-31 ("no only use create case form only... from all the profiles"); the field-generation spec's own mention of an "Invites" form is not used anywhere.
- **No Google Maps API key yet** — confirmed 2026-07-31; `calcDistanceETA()`/`calc4PointRoundtrip()` in all three new widgets are Haversine straight-line stand-ins (assumed 30 km/h average speed for a rough ETA) until a key is provided.
- **Vendor widget scope** — confirmed 2026-07-31: an individual vendor does the full on-road flow themselves (same screens as Technician/Driver, branched by `Service_Type`); a fleet vendor hands off to one of their own technicians instead, after accepting.
